\documentclass{IEEEtran}
\usepackage{cite}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{url}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage[table]{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,backgrounds}
\usepackage{pdflscape}
\usepackage{array}
\usepackage{tabularx}
\usepackage[hidelinks]{hyperref}

\definecolor{phase1blue}{RGB}{52,152,219}
\definecolor{phase2green}{RGB}{46,204,113}
\definecolor{phase3orange}{RGB}{230,126,34}
\definecolor{phase4purple}{RGB}{155,89,182}

\title{{\LARGE\textbf{Architecture of a Reproducible CRISP-DM Review Pipeline}}\\
{\Large\textit{Inherited Quantum-Storage, Notebook, and Validation Workflow}}\\
{\large\texttt{DSCI 601 Architecture Report}}}

\author{%
\begin{minipage}{0.48\textwidth}\begin{center}
{\bf Piter Z. Garcia}\\
\begin{small}
MS Data Science / Decision-Making \& Algorithmic Fairness\\
Rochester Institute of Technology (RIT)\\
{\it pizg8794@g.rit.edu}
\end{small}
\end{center}\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}\begin{center}
{\bf DSCI 601 Project Context}\\
\begin{small}
Architecture deliverable aligned with code review,
reproducibility, and inherited quantum-project tooling
\end{small}
\end{center}\end{minipage}%
}

\begin{document}
\maketitle

\section{Architecture Overview}

This report presents the project architecture as a reproducible,
CRISP-DM-aligned system built on top of the existing
\texttt{quantum\_project} infrastructure. The purpose of the report is to
identify the major layers of the system, document the components that implement
each stage, and provide enough workflow detail for another party to recreate the
software, execute the pipeline, and replicate the reported results.

The inherited quantum workflow provides the main execution structure used here:
shared-drive storage, notebook-based analysis, evaluator-owned summaries,
runner-based experiment execution, and state-analysis postprocessing. The
current repository extends that structure by adding a review-oriented control
layer and a fairness--performance synthesis layer that compares fairness
criteria against inherited performance metrics.

The architecture is organized around five concerns: CRISP-DM context, control
and validation, execution stages, fairness--performance synthesis, and artifacts
and replication outputs.

\section{Recreating the System and Replicating Results}

The architecture supports three entry paths so that another party can recreate
the system and reproduce the reported behavior.

\begin{table}[ht!]
\centering
\caption{Supported execution and replication paths inherited from the quantum project}
\label{tab:paths}
\begin{tabular}{p{1.8cm}p{2.8cm}p{2.7cm}}
\toprule
\textbf{Path} & \textbf{How it is started} & \textbf{Purpose} \\
\midrule
Colab / shared notebook & open shared notebook, mount Drive, run notebook cells & fastest replication of presented results \\
Local installation & clone repo, create virtual environment, install dependencies, run scripts or notebooks & development and reproducible local validation \\
GCP VM / batch path & startup script plus batch runner scripts & large runs, longer experiments, remote replication \\
\bottomrule
\end{tabular}
\end{table}

The notebook path is the fastest route to replication because the shared
notebooks already mount Drive, install dependencies, load saved evaluator or
framework state, and write results back to the shared infrastructure. The local
path remains necessary for repository review, testing, and debugging. The GCP
path extends the same workflow to longer or larger runs without changing the
underlying component structure.

These entry paths lead into the same inherited execution chain. They do not
create separate systems. For that reason, the report documents setup, execution,
interpretation, and sharing as part of the architecture rather than as
supplementary instructions.

\section{System Architecture}

Figure~\ref{fig:architecture_draft2} summarizes the full workflow. At the
highest level, Business Understanding and Data Understanding remain contextual
layers rather than CI stages. The control layer receives repository events or
user entry paths, applies configuration and validation rules, and drives the
execution stages. The execution layer performs collection, cleaning,
preparation, exploration, mining, and evaluation. The fairness--performance
layer then combines inherited performance summaries with repository-side
fairness criteria. Finally, the artifacts layer preserves the evidence produced
by each stage.

The figure is placed on its own landscape page at the end of the report so that
all layers, arrows, and stage labels remain legible.

\section{Stage-to-Component Mapping}

Table~\ref{tab:stage_objects} identifies the quantum-side components associated
with each stage and specifies their operational role within the workflow.

\begin{table*}[!t]
\centering
\caption{Stage-to-component mapping for the inherited review and replication workflow}
\label{tab:stage_objects}
\small
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{1.7cm} >{\raggedright\arraybackslash}p{3.3cm} >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X}
\toprule
\textbf{Stage} & \textbf{Component / quantum object} & \textbf{Description} & \textbf{Usage} \\
\midrule
Data Collection & \texttt{LocalBackupManager}, \texttt{GoogleDriveBackupManager}, registry build / latest-state lookup & resolves shared-drive paths and retrieves matching framework or model state from \texttt{quantum\_data\_lake/} & notebooks, local scripts, and GCP runs begin by scanning the registry and loading compatible saved state \\
Data Cleaning & \texttt{MultiRunEvaluator}, \texttt{\_infer\_models\_from\_state()}, \texttt{\_\_eq\_\_()}, \texttt{ensure\_summary\_contract()} & validates state compatibility, normalizes scenario storage, and repairs missing evaluator summary payloads when needed & collection outputs are filtered into evaluator-valid state before analysis, plotting, or summary generation \\
Data Preparation / Exploration & \texttt{state\_analysis.py}, evaluator \texttt{scenarios\_stats}, evaluator \texttt{scenarios\_results} payloads & consumes evaluator-produced summaries and restructures them into flattened or scenario-oriented tables & notebooks and scripts use these outputs for master datasets, scenario summaries, and replication-ready tables \\
Modeling / Mining & \texttt{AllocatorRunner}, \texttt{QuantumExperimentRunner}, allocator objects & executes runs, assigns route qubit allocations, and produces model-level outputs, traces, and experiment artifacts & local, notebook, and GCP paths use the runner chain to generate comparable outputs across scenarios \\
Evaluation & \texttt{MultiRunEvaluator.calculate\_scenario\_winner()}, \texttt{calculate\_scenario\_performance()}, \texttt{calculate\_scenarios\_performance()} & computes winner counts, average gap, average efficiency, oracle reward, and scenario-level summaries & turns raw runner outputs into reviewer-visible performance evidence and comparison payloads \\
Fairness--Performance Synthesis & repository-side fairness--performance component, allocator context, evaluator metrics, runner outputs, \texttt{QuantumEvaluatorVisualizer}, robustness plotting path & combines inherited performance metrics with fairness criteria and produces tradeoff views & used after evaluation to generate fairness-versus-performance tables, plots, and reviewer-facing comparisons \\
Review Delivery / Replication Outputs & notebooks, \texttt{visualizer.py}, shared-drive exports, \texttt{pipeline\_manifest.json} & publishes figures, tables, notebook-ready outputs, and provenance artifacts & collaborators rerun notebooks and inspect shared-drive artifacts to reproduce, interpret, and share results \\
\bottomrule
\end{tabularx}
\end{table*}

\section{Detailed Description of Each Stage}

\subsection{Business Understanding}
Business Understanding defines the research objective, the fairness criteria,
the acceptance rules, and the replication target. It establishes what the
system is expected to optimize and what evidence must be produced before results
are considered review-ready.

\subsection{Data Understanding}
Data Understanding defines the structure of the shared-drive data lake, the
semantics of the saved state, and the assumptions that notebooks and analysis
scripts make about evaluator outputs. The repository depends on a real upstream
storage model rather than an isolated local file layout.

\subsection{Data Collection Stage}
\textbf{Components.} The collection stage is built from
\texttt{LocalBackupManager}, \texttt{GoogleDriveBackupManager}, and the
registry-based latest-state lookup path listed in Table~\ref{tab:stage_objects}.

\textbf{How they work together.} \texttt{GoogleDriveBackupManager} resolves the
shared-drive source and identifies the relevant storage root.
\texttt{LocalBackupManager} provides a local-facing wrapper around that storage
path. The registry logic then indexes the available saved states and selects the
latest compatible artifact for the requested component or run. Together, these
parts convert the shared-drive namespace into a concrete set of executable
inputs for the downstream pipeline.

\textbf{Output.} The stage produces source references and retrieved framework or
model states that can be passed into evaluator-based validation.

\subsection{Data Cleaning Stage}
\textbf{Components.} The cleaning stage is centered on
\texttt{MultiRunEvaluator}, supported by \texttt{\_infer\_models\_from\_state()},
\texttt{\_\_eq\_\_()}, and \texttt{ensure\_summary\_contract()}.

\textbf{How they work together.} The retrieved state from the collection stage
is first inspected by \texttt{\_infer\_models\_from\_state()} so that the
system can recover the model identity and expected evaluator structure.
\texttt{\_\_eq\_\_()} and related evaluator-side checks verify structural
compatibility. When evaluator summaries are incomplete or not normalized,
\texttt{ensure\_summary\_contract()} repairs the summary shape so that later
analysis stages consume a stable payload. Together, these components transform
raw retrieved state into evaluator-valid state.

\textbf{Output.} The output is a cleaned and contract-consistent evaluator state
that downstream analysis can trust.

\subsection{Data Preparation and Exploration Stage}
\textbf{Components.} This stage is built from \texttt{state\_analysis.py} and
the evaluator-owned \texttt{scenarios\_stats} and
\texttt{scenarios\_results} payloads referenced in
Table~\ref{tab:stage_objects}.

\textbf{How they work together.} \texttt{MultiRunEvaluator} provides the
scenario summaries and normalized result structures. \texttt{state\_analysis.py}
then consumes those evaluator outputs and restructures them into flattened
tables, scenario-oriented summaries, and analysis-ready views. The stage is
therefore a composition of evaluator-generated summaries plus state-analysis
postprocessing rather than an independent replacement for evaluator logic.

\textbf{Output.} The output is a prepared set of scenario summaries, tables,
and exploratory artifacts that support mining, notebooks, and reviewer
inspection.

\subsection{Modeling and Mining Stage}
\textbf{Components.} The mining stage is implemented through
\texttt{AllocatorRunner}, \texttt{QuantumExperimentRunner}, and the allocator
objects responsible for route qubit allocation.

\textbf{How they work together.} The allocator objects define the decision
logic used within each run. \texttt{QuantumExperimentRunner} executes the
underlying experiment path and produces run-level outputs and traces.
\texttt{AllocatorRunner} orchestrates the higher-level execution across
allocators, scenarios, and configurations. Together, these components produce
the substantive outputs that the evaluator later summarizes.

\textbf{Output.} The stage produces model outputs, traces, and experiment
artifacts suitable for scenario-level comparison.

\subsection{Evaluation Stage}
\textbf{Components.} The evaluation stage is owned by
\texttt{MultiRunEvaluator}, particularly through
\texttt{calculate\_scenario\_winner()},
\texttt{calculate\_scenario\_performance()}, and
\texttt{calculate\_scenarios\_performance()}.

\textbf{How they work together.} The evaluator consumes the runner outputs,
organizes them by scenario, and computes the summary statistics required for
comparison: winner counts, average gap, average efficiency, oracle reward, and
related scenario-level measures. This stage is deliberately evaluator-owned so
that judgment logic remains close to the object that also maintains the state
contract.

\textbf{Output.} The output is a reviewer-visible performance summary that can
be consumed directly or passed into the fairness--performance layer.

\subsection{Fairness--Performance Synthesis Layer}
\textbf{Components.} This new layer combines a repository-side
fairness--performance component with allocator context, runner outputs,
evaluator metrics, and visualizer or robustness-plotting consumers such as
\texttt{QuantumEvaluatorVisualizer}.

\textbf{How they work together.} The runner chain and allocator context define
how performance was produced. The evaluator computes the inherited performance
metrics. The fairness--performance component then places those metrics beside the
fairness criteria defined at the repository level and produces tradeoff views
that neither the raw runner nor the raw evaluator produces on its own. The
visualizer path then consumes those combined outputs to generate figures and
comparative summaries.

\textbf{Output.} The output is a set of tradeoff tables, comparative plots, and
review-facing fairness-versus-performance summaries.

\subsection{Review Delivery and Replication Outputs}
\textbf{Components.} The final stage uses notebooks, \texttt{visualizer.py},
shared-drive exports, and \texttt{pipeline\_manifest.json}.

\textbf{How they work together.} The prior stages generate the saved state,
metrics, fairness summaries, and plots. The notebook and visualizer path turns
those into human-readable artifacts. The manifest records provenance for the
run. Shared-drive export keeps the outputs accessible to collaborators and later
reanalysis.

\textbf{Output.} The output is a set of figures, tables, rerunnable notebook
artifacts, and provenance records that support review and replication.

\section{User Workflow: Setup, Run, Interpret, and Share}

\subsection{Setup and Installation}
Users can enter through three supported paths.

\textbf{Notebook path.} Open the shared notebook, mount Google Drive, install
dependencies in the notebook runtime, and run the relevant cells.

\textbf{Local path.} Clone the repository, create a virtual environment,
install dependencies from \texttt{requirements.txt}, and run the required
scripts or notebooks.

\textbf{GCP path.} Provision or enter the VM, run the startup script, and then
use the batch or dynamic runner scripts for longer experiments.

\subsection{How the User Runs the System}
Once setup is complete, the user selects a stage or full workflow. In the local
path, this usually means running the quick validation or full review path. In
the notebook path, it means configuring an experiment and executing the
evaluator-backed workflow. In the GCP path, it means invoking startup and batch
scripts and letting the run complete remotely.

\subsection{How the User Interprets Results}
Results are interpreted through the same artifact conventions used by the
upstream project. Framework-state and model-state outputs are written under the
shared data lake, typically in day-partitioned directories. Notebooks,
visualizers, evaluator summaries, and fairness--performance tables then consume
those saved artifacts to generate comparisons and plots.

\subsection{How the User Shares Results}
Because the data lake is shared-drive based, completed runs can be made visible
to collaborators without a separate export pipeline. Results written to
\texttt{quantum\_data\_lake/}, including framework state, model state, tables,
and figures, are already placed in a location that supports team inspection and
notebook-based reanalysis.

\clearpage
\begin{landscape}
\thispagestyle{plain}
\begin{figure}[p]
\centering
\resizebox{0.98\linewidth}{!}{\input{architecture_diagram.tex}}
\caption{Architecture of the inherited CRISP-DM review and replication pipeline.
Business Understanding and Data Understanding remain in the architectural
context, while the executable workflow starts with collection from the
shared-drive source layer. A separate fairness--performance synthesis layer
compares fairness criteria against inherited performance metrics before review
delivery and replication outputs are published.}
\label{fig:architecture_draft2}
\end{figure}
\end{landscape}

\end{document}