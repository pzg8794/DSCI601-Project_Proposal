\documentclass{IEEEtran}
\usepackage{cite}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{url}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage[table]{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,backgrounds}
\usepackage{pdflscape}
\usepackage{array}
\usepackage{tabularx}
\usepackage[hidelinks]{hyperref}
\usepackage{array,tabularx,makecell}

% \usepackage{array}
% \usepackage{makecell}
% \usepackage{url}
\urlstyle{tt}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash\hspace{0pt}}p{#1}}
\newcommand{\code}[1]{\nolinkurl{#1}}

\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}

\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.12}

\definecolor{phase1blue}{RGB}{52,152,219}
\definecolor{phase2green}{RGB}{46,204,113}
\definecolor{phase3orange}{RGB}{230,126,34}
\definecolor{phase4purple}{RGB}{155,89,182}

\title{{\LARGE\textbf{Architecture of a Reproducible CRISP-DM Review Pipeline}}\\
{\Large\textit{Inherited Quantum-Storage, Notebook, and Validation Workflow}}\\
{\large\texttt{DSCI 601 Architecture Report}}}

\author{%
\begin{minipage}{0.48\textwidth}\begin{center}
{\bf Piter Z. Garcia}\\
\begin{small}
MS Data Science / Decision-Making \& Algorithmic Fairness\\
Rochester Institute of Technology (RIT)\\
{\it pizg8794@g.rit.edu}
\end{small}
\end{center}\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}\begin{center}
{\bf DSCI 601 Project Context}\\
\begin{small}
Architecture deliverable aligned with code review,
reproducibility, and inherited quantum-project tooling
\end{small}
\end{center}\end{minipage}%
}

\begin{document}
\maketitle

\section{Architecture Overview}

This work describes the project's architecture as a reproducible,
CRISP-DM-aligned system built on top of the existing
\texttt{quantum\_project} infrastructure. The goal is to describe the code
organization while providing sufficient architectural and workflow detail for
users to recreate the system, run the code, understand each stage, and
replicate the results presented.

The inherited quantum workflow reuses the same shared-drive storage, notebook
path, evaluator logic, runner path, and state-analysis conventions that already
produce reproducible artifacts in the upstream project. The work treats
replication as part of the architecture itself rather than as a separate
appendix.

The architecture is organized around five interacting concerns: high-level
CRISP-DM context, a control layer that governs execution and validation, an
execution layer that performs collection through evaluation, a
fairness--performance synthesis layer, and an artifacts layer that preserves
provenance and reviewer-visible evidence.

\section{Recreating the System and Replicating Results}

To allow users to recreate the software and replicate results, the architecture supports three entry paths rather than one.

\begin{table}[ht!]
\centering
\caption{Supported execution and replication paths inherited from the quantum project}
\label{tab:paths}
\begin{tabular}{p{1.8cm}p{2.8cm}p{2.7cm}}
\toprule
\textbf{Path} & \textbf{How it is started} & \textbf{Purpose} \\
\midrule
Colab / shared notebook & open shared notebook, mount Drive, run notebook cells & fastest replication of presented results \\
Local installation & clone repo, create virtual environment, install dependencies, run scripts or notebooks & development and reproducible local validation \\
GCP VM / batch path & startup script plus batch runner scripts & large runs, longer experiments, remote replication \\
\bottomrule
\end{tabular}
\end{table}

The shared notebook is the fastest route to result replication because it already mounts the Drive, installs dependencies, loads saved evaluator or framework state, and writes results back to the same shared infrastructure. The local path remains necessary because a reviewer or developer must also be able to install the software, run tests, and inspect outputs without relying on Colab. The GCP path extends the same architecture to a remote execution context for larger or longer runs.

These paths do not lead to different systems, but different entry points into the same inherited execution structure. That is why this work explains how users set up the environment, run the code, interpret results, and share outputs.

\section{System Architecture}

Figure~\ref{fig:architecture_draft2} presents the architecture as a technical
summary of the full workflow. The figure should be read from left to right and
from top to bottom.

At the highest level, Business and Data Understanding remain in an
architectural context that defines the project objective, fairness
interpretation, data expectations, and replication target. The control layer
receives repository events or user entry paths, applies configuration and
validation rules, and drives the actual execution stages. The execution layer
performs collection, cleaning, preparation, exploration, mining, and
evaluation. Beneath that, the fairness--performance synthesis layer combines
inherited performance summaries with repository-side fairness criteria. Finally,
the artifacts layer preserves the evidence produced by each stage.

% Because the figure is dense, it is placed on its own landscape page at the end of the report.

\section{Stage-to-Component Mapping}

Table~\ref{tab:stage_objects} maps each stage to the quantum-side components
used to build and execute it.

\begin{table*}[!t]
\centering
\caption{Stage-to-component mapping for the inherited review and replication workflow}
\label{tab:stage_objects}
\scriptsize
\begin{tabular}{L{2.0cm} L{4.2cm} L{5.0cm} L{5.0cm}}
\toprule
\textbf{Stage} & \textbf{Component} & \textbf{Description} & \textbf{Usage} \\
\midrule

Data Collection &
\makecell[tl]{
\code{LocalBackupManager}\\
\code{GoogleDriveBackupManager}\\
registry build / latest-state lookup
} &
resolves shared-drive paths and retrieves matching framework or model state from
\code{quantum_data_lake/} &
notebooks, local scripts, and GCP runs begin by scanning the registry and loading
compatible saved state \\
\addlinespace[2pt]

Data Cleaning &
\makecell[tl]{
\code{MultiRunEvaluator}\\
\code{_infer_models_from_state()}\\
\code{__eq__()}\\
\code{ensure_summary_contract()}
} &
validates state compatibility, normalizes scenario storage, and repairs missing
evaluator summary payloads when needed &
collection outputs are filtered into evaluator-valid state before analysis,
plotting, or summary generation \\
\addlinespace[2pt]

\makecell[tl]{Data Preparation /\\ Exploration} &
\makecell[tl]{
\code{state_analysis.py}\\
evaluator \code{scenarios_stats}\\
evaluator \code{scenarios_results} payloads
} &
consumes evaluator-produced summaries and restructures them into flattened or
scenario-oriented tables &
notebooks and scripts use these outputs for master datasets, scenario summaries,
and replication-ready tables \\
\addlinespace[2pt]

Modeling / Mining &
\makecell[tl]{
\code{AllocatorRunner}\\
\code{QuantumExperimentRunner}\\
allocator objects
} &
executes runs, assigns route qubit allocations, and produces model-level outputs,
traces, and experiment artifacts &
local, notebook, and GCP paths use the runner chain to generate comparable
outputs across scenarios \\
\addlinespace[2pt]

Evaluation &
\makecell[tl]{
\code{MultiRunEvaluator.calculate_scenario_winner()}\\
\code{calculate_scenario_performance()}\\
\code{calculate_scenarios_performance()}
} &
computes winner counts, average gap, average efficiency, oracle reward, and
scenario-level summaries &
turns raw runner outputs into reviewer-visible performance evidence and
comparison payloads \\
\addlinespace[2pt]

\makecell[tl]{Fairness--Performance\\ Synthesis} &
\makecell[tl]{
repository-side fairness--performance component\\
allocator context\\
evaluator metrics\\
runner outputs\\
\code{QuantumEvaluatorVisualizer}\\
robustness plotting path
} &
combines inherited performance metrics with fairness criteria and produces
tradeoff views &
used after evaluation to generate fairness-versus-performance tables, plots, and
reviewer-facing comparisons \\
\addlinespace[2pt]

\makecell[tl]{Review Delivery /\\ Replication Outputs} &
\makecell[tl]{
notebooks\\
\code{visualizer.py}\\
shared-drive exports\\
\code{pipeline_manifest.json}
} &
publishes figures, tables, notebook-ready outputs, and provenance artifacts &
collaborators rerun notebooks and inspect shared-drive artifacts to reproduce,
interpret, and share results \\
\bottomrule
\end{tabular}
\end{table*}

\section{Detailed Description of Each Stage}

\subsection{Business Understanding}
Business Understanding defines the project objective, the fairness criteria, the
acceptance rules, and the replication target. This stage determines what counts
as success and which outputs must exist before work is considered review-ready.

\subsection{Data Understanding}
Data Understanding characterizes the shared-drive storage model, the semantics
of saved state, and the assumptions that notebooks and downstream analysis make
about evaluator outputs. The repository is not inventing a synthetic storage
model; it depends on the real upstream data lake.

\subsection{Data Collection Stage}
\textbf{Components.} The collection stage is built from
\texttt{LocalBackupManager}, \texttt{GoogleDriveBackupManager}, and the
registry-based latest-state lookup path listed in Table~\ref{tab:stage_objects}.

\textbf{Linkage.} \texttt{GoogleDriveBackupManager} resolves the shared-drive
source and identifies the relevant storage root. \texttt{LocalBackupManager}
provides the local-facing wrapper around that storage path. The registry logic
then indexes the available saved states and selects the latest compatible
artifact for the requested component or run. Together, these parts convert the
shared-drive namespace into a concrete set of executable inputs for the
pipeline.

\textbf{Output.} The stage produces source references and retrieved framework or
model states that can be passed into evaluator-based validation.

\subsection{Data Cleaning Stage}
\textbf{Components.} The cleaning stage is centered on
\texttt{MultiRunEvaluator}, supported by \texttt{\_infer\_models\_from\_state()},
\texttt{\_\_eq\_\_()}, and \texttt{ensure\_summary\_contract()}.

\textbf{Linkage.} The retrieved state from the collection stage is first
inspected by \texttt{\_infer\_models\_from\_state()} so that the system can
recover the model identity and expected evaluator structure. \texttt{\_\_eq\_\_()}
and related evaluator-side checks verify structural compatibility.
When evaluator summaries are incomplete or not normalized,
\texttt{ensure\_summary\_contract()} repairs the summary shape so that later
analysis stages consume a stable payload. Together, these components transform
raw retrieved state into evaluator-valid state.

\textbf{Output.} The output is a cleaned and contract-consistent evaluator state
that downstream analysis can trust.

\subsection{Data Preparation and Exploration Stage}
\textbf{Components.} This stage is built from \texttt{state\_analysis.py} and
the evaluator-owned \texttt{scenarios\_stats} and
\texttt{scenarios\_results} payloads referenced in
Table~\ref{tab:stage_objects}.

\textbf{Linkage.} \texttt{MultiRunEvaluator} provides the scenario summaries
and normalized result structures. \texttt{state\_analysis.py} then consumes
those evaluator outputs and restructures them into flattened tables,
scenario-oriented summaries, and analysis-ready views. The stage is therefore a
composition of evaluator-generated summaries plus state-analysis postprocessing
rather than an independent replacement for evaluator logic.

\textbf{Output.} The output is a prepared set of scenario summaries, tables,
and exploratory artifacts that support mining, notebooks, and reviewer
inspection.

\subsection{Modeling and Mining Stage}
\textbf{Components.} The mining stage is implemented through
\texttt{AllocatorRunner}, \texttt{QuantumExperimentRunner}, and the allocator
objects responsible for route qubit allocation.

\textbf{Linkage.} The allocator objects define the decision logic used within
each run. \texttt{QuantumExperimentRunner} executes the underlying experiment
path and produces run-level outputs and traces. \texttt{AllocatorRunner}
orchestrates the higher-level execution across allocators, scenarios, and
configurations. Together, these components produce the substantive outputs that
the evaluator later summarizes.

\textbf{Output.} The stage produces model outputs, traces, and experiment
artifacts suitable for scenario-level comparison.

\subsection{Evaluation Stage}
\textbf{Components.} The evaluation stage is owned by
\texttt{MultiRunEvaluator}, particularly through
\texttt{calculate\_scenario\_winner()},
\texttt{calculate\_scenario\_performance()}, and
\texttt{calculate\_scenarios\_performance()}.

\textbf{Linkage.} The evaluator consumes the runner outputs, organizes them by
scenario, and computes the summary statistics required for comparison: winner
counts, average gap, average efficiency, oracle reward, and related
scenario-level measures. This stage keeps judgment logic close to the same
object that also maintains the state contract.

\textbf{Output.} The output is a reviewer-visible performance summary that can
be consumed directly or passed into the fairness--performance layer.

\subsection{Fairness--Performance Synthesis Layer}
\textbf{Components.} This new layer combines a repository-side
fairness--performance component with allocator context, runner outputs,
evaluator metrics, and visualizer or robustness-plotting consumers such as
\texttt{QuantumEvaluatorVisualizer}.

\textbf{Linkage.} The runner chain and allocator context define how
performance was produced. The evaluator computes the inherited performance
metrics. The fairness--performance component then places those metrics beside
the fairness criteria defined at the repository level and produces tradeoff
views that neither the raw runner nor the raw evaluator produces on its own.
The visualizer path then consumes those combined outputs to generate figures and
comparative summaries.

\textbf{Output.} The output is a set of tradeoff tables, comparative plots, and
review-facing fairness-versus-performance summaries.

\subsection{Review Delivery and Replication Outputs}
\textbf{Components.} The final stage uses notebooks, \texttt{visualizer.py},
shared-drive exports, and \texttt{pipeline\_manifest.json}.

\textbf{Linkage.} The prior stages generate the saved state, metrics,
fairness summaries, and plots. The notebook and visualizer path turns those
into human-readable artifacts. The manifest records provenance for the run.
Shared-drive export keeps the outputs accessible to collaborators and later
reanalysis.

\textbf{Output.} The output is a set of figures, tables, rerunnable notebook
artifacts, and provenance records that support review and replication.

\section{User Workflow: Setup, Run, Interpret, and Share}

\subsection{Setup and Installation}
Users can interact with the system through three supported paths.

\textbf{Notebook path.} Open the shared notebook, mount Google Drive, install
dependencies in the notebook runtime, and run the relevant cells.

\textbf{Local path.} Clone the repository, create a virtual environment,
install dependencies from \texttt{requirements.txt}, and run the required
scripts or notebooks.

\textbf{GCP path.} Provision or enter a VM, run the startup script, and then
use the batch or dynamic runner scripts for longer experiments.

\subsection{How the User Runs the System}
Once setup is complete, the user chooses a stage or full workflow. In the local
path, that usually means running the quick test or full validation script. In
the notebook path, it means configuring an experiment in the notebook and
executing the evaluator-backed workflow. In the GCP path, it means invoking the
startup and batch scripts and allowing the run to complete remotely.

These paths remain different entry points into the same inherited execution
structure. They do not define different systems.

\subsection{How the User Interprets Results}
The user interprets results through the same artifact conventions used by the
upstream project. Framework-state and model-state outputs are written under the
shared data lake, often in day-partitioned directories. Notebooks and visualizer
logic then consume those results to generate plots, scenario comparisons, and
summary tables.

\subsection{How the User Shares Results}
Because the data lake is shared-drive based, completed runs can be made visible
to collaborators without a separate export pipeline. Results written to
\texttt{quantum\_data\_lake/}, including framework state, model state, tables,
and figures, are already placed in a location that supports team inspection and
notebook-based reanalysis.

\clearpage
\begin{landscape}
\thispagestyle{plain}
\begin{figure}[p]
\centering
\resizebox{0.98\linewidth}{!}{\input{architecture_diagram.tex}}
\caption{Architecture of the inherited CRISP-DM review and replication pipeline.
Business Understanding and Data Understanding remain in the architectural
context, while the executable workflow starts with collection from the
shared-drive source layer. A separate fairness--performance synthesis layer
compares fairness criteria against inherited performance metrics before review
delivery and replication outputs are published.}
\label{fig:architecture_draft2}
\end{figure}
\end{landscape}

\end{document}